# Pakai image prebuilt dari Docker Hub, jangan build dari source.
# (docker-compose.override.yml sengaja tidak diikutkan)
COMPOSE_FILE=docker-compose.yml:docker-compose.stbi.yml
COMPOSE_PROJECT_NAME=openmrs
TAG=qa
